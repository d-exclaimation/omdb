name: zip and upload

on:
  push:
    branches:
      - main
jobs:
  zip:
    container:
      image: node:latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls -l
      - run: npm run zip
      - uses: actions/upload-artifact@v2
        with:
          name: vno16
          path: vno16.zip

